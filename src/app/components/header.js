import "./header.css";

export default function Header() {
    return (
        <header className=" bg-red-300 w-full h-[150px] " >
            <div className=" w-full h-full bg-blue-600 text-[10vmin]">
                I am Header.
            </div>
        </header>
    );
}
